# Explainability Report

Generated by `pnpm governance`. For every generated screen this report
shows the chain from request to rendered UI: the interpreted intent, the
rules retrieved for each step, the constraints checked, and the outcome.
This is what a human reviewer inspects to audit an agent decision.

## Account settings

- **User request:** "change my account settings and maybe delete my account"
- **Interpreted intent:** flow `account-settings` (match score 13)
- **Goal:** Let a user view and change profile, notification, and account data - including the guarded, destructive deletion of the account.

### Step: settings

Retrieved rules (11): `rule-action-label-states-action`, `rule-danger-zone-separation`, `rule-destructive-dialog-shape`, `rule-destructive-needs-confirmation`, `rule-error-announce`, `rule-error-recovery`, `rule-field-has-label`, `rule-focus-visible`, `rule-responsive-single-column`, `rule-single-primary-action`, `rule-field-autocomplete`

Constraints checked: `label-states-action`, `danger-zone-separated`, `destructive-dialog-shape`, `destructive-needs-confirmation`, `error-has-recovery`, `field-has-label`, `forms-single-column`, `single-primary-action`, `field-has-autocomplete`

Why the screen looks like this:
- primary-action: exactly one primary action (the step's submit)
- responsive: form is single-column so it reflows on small screens
- destructive-action: danger action is isolated in a danger zone and guarded by a destructive confirmation dialog

Outcome: **ACCEPTED** (0 errors, 0 warnings)

## Checkout

- **User request:** "I want to buy the items in my cart and pay"
- **Interpreted intent:** flow `checkout` (match score 6.5)
- **Goal:** Let a user pay for the items in their cart with full transparency of costs before the final commitment.

### Step: shipping

Retrieved rules (9): `rule-action-label-states-action`, `rule-checkout-preserve-input`, `rule-error-announce`, `rule-error-recovery`, `rule-field-has-label`, `rule-focus-visible`, `rule-responsive-single-column`, `rule-single-primary-action`, `rule-field-autocomplete`

Constraints checked: `label-states-action`, `error-has-recovery`, `field-has-label`, `forms-single-column`, `single-primary-action`, `field-has-autocomplete`

Why the screen looks like this:
- primary-action: exactly one primary action (the step's submit)
- responsive: form is single-column so it reflows on small screens

Outcome: **ACCEPTED** (0 errors, 0 warnings)

### Step: payment

Retrieved rules (10): `rule-action-label-states-action`, `rule-checkout-preserve-input`, `rule-error-announce`, `rule-error-recovery`, `rule-field-has-label`, `rule-focus-visible`, `rule-responsive-single-column`, `rule-sensitive-redaction`, `rule-single-primary-action`, `rule-field-autocomplete`

Constraints checked: `label-states-action`, `error-has-recovery`, `field-has-label`, `forms-single-column`, `single-primary-action`, `field-has-autocomplete`

Why the screen looks like this:
- primary-action: exactly one primary action (the step's submit)
- responsive: form is single-column so it reflows on small screens

Outcome: **ACCEPTED** (0 errors, 0 warnings)

### Step: review

Retrieved rules (9): `rule-action-label-states-action`, `rule-checkout-preserve-input`, `rule-cost-transparency`, `rule-error-announce`, `rule-error-recovery`, `rule-explicit-consent`, `rule-focus-visible`, `rule-responsive-single-column`, `rule-single-primary-action`

Constraints checked: `label-states-action`, `commit-shows-summary`, `error-has-recovery`, `label-states-consequence`, `forms-single-column`, `single-primary-action`

Why the screen looks like this:
- cost-transparency: rendered an order summary before the commit action
- primary-action: exactly one primary action (the step's submit)

Outcome: **ACCEPTED** (0 errors, 0 warnings)

## User onboarding

- **User request:** "I want to sign up and create a new account"
- **Interpreted intent:** flow `onboarding` (match score 4.5)
- **Goal:** Let a new user create an account with the minimum information needed, building trust step by step.

### Step: account

Retrieved rules (10): `rule-action-label-states-action`, `rule-error-announce`, `rule-error-recovery`, `rule-field-has-label`, `rule-focus-visible`, `rule-responsive-single-column`, `rule-single-primary-action`, `rule-onboarding-defer`, `rule-field-autocomplete`, `rule-minimal-input`

Constraints checked: `label-states-action`, `error-has-recovery`, `field-has-label`, `forms-single-column`, `single-primary-action`, `field-has-autocomplete`, `minimal-fields`

Why the screen looks like this:
- primary-action: exactly one primary action (the step's submit)
- responsive: form is single-column so it reflows on small screens
- minimal-input: form asks for only 2 field(s)

Outcome: **ACCEPTED** (0 errors, 0 warnings)

### Step: profile

Retrieved rules (10): `rule-action-label-states-action`, `rule-error-announce`, `rule-error-recovery`, `rule-field-has-label`, `rule-focus-visible`, `rule-responsive-single-column`, `rule-single-primary-action`, `rule-skippable-optional`, `rule-onboarding-defer`, `rule-field-autocomplete`

Constraints checked: `label-states-action`, `error-has-recovery`, `field-has-label`, `forms-single-column`, `single-primary-action`, `optional-step-skippable`, `field-has-autocomplete`

Why the screen looks like this:
- primary-action: exactly one primary action (the step's submit)
- skippable-step: added a secondary Skip action
- responsive: form is single-column so it reflows on small screens

Outcome: **ACCEPTED** (0 errors, 0 warnings)

## Password reset

- **User request:** "I forgot my password and I'm locked out"
- **Interpreted intent:** flow `password-reset` (match score 9.25)
- **Goal:** Let a locked-out user regain access safely, without revealing whether an email is registered.

### Step: request

Retrieved rules (10): `rule-action-label-states-action`, `rule-error-announce`, `rule-error-recovery`, `rule-field-has-label`, `rule-focus-visible`, `rule-no-account-disclosure`, `rule-responsive-single-column`, `rule-single-primary-action`, `rule-field-autocomplete`, `rule-minimal-input`

Constraints checked: `label-states-action`, `error-has-recovery`, `field-has-label`, `forms-single-column`, `single-primary-action`, `field-has-autocomplete`, `minimal-fields`

Why the screen looks like this:
- primary-action: exactly one primary action (the step's submit)
- responsive: form is single-column so it reflows on small screens
- minimal-input: form asks for only 1 field(s)

Outcome: **ACCEPTED** (0 errors, 0 warnings)

### Step: new-password

Retrieved rules (8): `rule-action-label-states-action`, `rule-error-announce`, `rule-error-recovery`, `rule-field-has-label`, `rule-focus-visible`, `rule-responsive-single-column`, `rule-single-primary-action`, `rule-field-autocomplete`

Constraints checked: `label-states-action`, `error-has-recovery`, `field-has-label`, `forms-single-column`, `single-primary-action`, `field-has-autocomplete`

Why the screen looks like this:
- primary-action: exactly one primary action (the step's submit)
- responsive: form is single-column so it reflows on small screens

Outcome: **ACCEPTED** (0 errors, 0 warnings)

## Subscription upgrade

- **User request:** "upgrade my plan to pro"
- **Interpreted intent:** flow `subscription-upgrade` (match score 8.5)
- **Goal:** Let a user move to a higher plan with a clear price difference and explicit confirmation of the new recurring charge.

### Step: choose-plan

Retrieved rules (8): `rule-action-label-states-action`, `rule-checkout-preserve-input`, `rule-cost-transparency`, `rule-error-announce`, `rule-error-recovery`, `rule-focus-visible`, `rule-responsive-single-column`, `rule-single-primary-action`

Constraints checked: `label-states-action`, `commit-shows-summary`, `error-has-recovery`, `forms-single-column`, `single-primary-action`

Why the screen looks like this:
- primary-action: exactly one primary action (the step's submit)
- responsive: form is single-column so it reflows on small screens

Outcome: **ACCEPTED** (0 errors, 0 warnings)

### Step: confirm-upgrade

Retrieved rules (10): `rule-action-label-states-action`, `rule-checkout-preserve-input`, `rule-cost-transparency`, `rule-error-announce`, `rule-error-recovery`, `rule-explicit-consent`, `rule-focus-visible`, `rule-recurring-disclosure`, `rule-responsive-single-column`, `rule-single-primary-action`

Constraints checked: `label-states-action`, `commit-shows-summary`, `error-has-recovery`, `label-states-consequence`, `summary-mentions-renewal`, `forms-single-column`, `single-primary-action`

Why the screen looks like this:
- cost-transparency: rendered an order summary before the commit action
- recurring-charge: summary states the renewal cadence
- primary-action: exactly one primary action (the step's submit)

Outcome: **ACCEPTED** (0 errors, 0 warnings)

## Rejected: malformed plan

A deliberately rule-violating AST was submitted. The validator **rejected** it (5 errors) and the renderer refused to render it:

- `error` **single-primary-action** @ `children[2]` — A screen may have at most one primary action; found 2 ("Submit", "Save").
- `error` **label-states-action** @ `children[0].children[1]` — Action label "Submit" does not state the action; name the outcome (e.g. "Create account", "Delete project").
- `error` **destructive-needs-confirmation** @ `children[1]` — Destructive action "Delete everything" must open a confirmation dialog (action.kind "open-dialog" targeting a destructive Dialog).
- `error` **danger-zone-separated** @ `children[1]` — Destructive action "Delete everything" must sit inside a danger-zone region, separated from routine controls.
- `warn` **field-has-autocomplete** @ `children[0].children[0]` — Field "email" (email) should set an autocomplete token to help password managers and assistive tech.
- `error` **forms-single-column** @ `children[0]` — Form "signup" must be single-column so it reflows on narrow viewports.

