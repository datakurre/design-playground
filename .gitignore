node_modules/
dist/
*.log
.devenv/
.devenv.flake.nix
.direnv/
.pre-commit-config.yaml
coverage/
playwright-browsers/
*.tsbuildinfo

# Regenerated by `pnpm db:seed` from the committed JSON sources; the
# pipeline also writes timestamped audit rows into it. The committed,
# human-readable artifacts live in scenarios/ and governance/.
db/design.db
db/design.db-journal
